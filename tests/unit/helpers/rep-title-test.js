
import { repTitle } from 'clicktocongress-ember/helpers/rep-title';
import { module, test } from 'qunit';

module('Unit | Helper | rep title');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = repTitle([42]);
  assert.ok(result);
});

